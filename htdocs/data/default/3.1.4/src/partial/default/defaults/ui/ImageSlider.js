/**
* ImageSlider 模块的默认配置
* @name ImageSlider.defaults
*/
define('ImageSlider.defaults', /**@lends ImageSlider.defaults*/ {

    /**
    * 生成的 id 的前缀。
    */
    prefix: 'KISP-ImageSlider-',

    /**
    * 给 img 元素生成的 id 的随机后缀的长度。
    */
    suffix: 4,

    volatile: true,
    opacity: 1,
    background: 'rgba(0, 0, 0, 0.8)',

    sample: '<img style="max-width: 100%; max-height: 100%;" src="{src}" />',


});

