/**
* Confirm 模块的默认配置
* @name Confirm.config
*/
define('Confirm.config', /**@lends Confirm.config*/ {

    /**
    * 组件高度。
    * 可以指定为百分比的字符串，或指定具体的数值（单位为像素），
    */
    height: 150,

    /**
    * 组件宽度。
    * 可以指定为百分比的字符串，或指定具体的数值（单位为像素），
    */
    width: 400,

});

